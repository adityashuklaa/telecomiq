# Deployable runtime deps — used by Streamlit Cloud AND the Docker image.
# Excludes the heavy torch + pyspark (see requirements-full.txt for those).
# In this configuration, Module 3 uses its IsolationForest fallback.
pandas==2.2.2
numpy==1.26.4
scikit-learn==1.5.1
xgboost==2.1.1
joblib==1.4.2
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
streamlit==1.38.0
plotly==5.24.1
anthropic==0.39.0
kafka-python==2.0.2
python-dotenv==1.0.1
requests==2.32.3
