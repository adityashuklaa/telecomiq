# Deployable runtime deps — used by Streamlit Cloud AND the Docker image.
# Excludes the heavy torch + pyspark (see requirements-full.txt for those).
# Minimum floors (>=) so pip resolves wheels for whatever Python version the
# host uses (Streamlit Cloud 3.12/3.13, Docker 3.11). Module 3 uses its
# IsolationForest fallback in this configuration.
pandas>=2.2.3
numpy>=1.26
scikit-learn>=1.5.1
xgboost>=2.1.1
joblib>=1.4
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.9
pydantic-settings>=2.5
streamlit>=1.38
plotly>=5.24
anthropic>=0.39
kafka-python>=2.0.2
python-dotenv>=1.0
requests>=2.32
