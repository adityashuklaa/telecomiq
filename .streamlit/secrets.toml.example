# Copy this to `.streamlit/secrets.toml` for LOCAL Streamlit runs, OR paste these
# keys into the Streamlit Community Cloud app's "Secrets" box when deploying.
# Never commit the real secrets.toml (it is git-ignored).

ANTHROPIC_API_KEY = "sk-ant-xxxxxxxxxxxxxxxxxxxxxxxx"
CLAUDE_MODEL = "claude-opus-4-8"
